# opencv-python-headless (e não opencv-python): o easyocr já depende da versão headless,
# e ter as duas instaladas causa conflito no módulo cv2.
opencv-python-headless>=4.10
numpy>=1.26
pillow>=10.4
pillow-heif>=0.18
pypdfium2>=4.30
easyocr>=1.7.2
streamlit>=1.50
pytest>=8.3
# só para gerar a apostila em PDF (scripts/gerar_apostila.py)
markdown>=3.6
pygments>=2.18
